# ComfyUI Image Manager Configuration
#
# This file was auto-generated from backend/.env.example
# Rename to .env to use.

# 서버 설정
PORT=1566
NODE_ENV=development

# 네트워크 바인딩 설정
# 0.0.0.0 = 모든 네트워크 인터페이스에서 접근 가능 (기본값, 권장)
# localhost = 로컬 컴퓨터에서만 접근 가능
BIND_ADDRESS=0.0.0.0

# 외부 IP 감지 활성화 (네트워크 접속 정보 표시용)
ENABLE_EXTERNAL_IP=false

# 프론트엔드 URL (Vite 개발서버 포트)
FRONTEND_URL=http://localhost:1577

# ============================================
# Runtime Path Configuration
# ============================================
# Configure where the application stores its data
# All paths below are OPTIONAL - if not set, defaults to executable location
#
# RUNTIME_BASE_PATH: Set base directory for all data (uploads, database, logs, etc.)
# If set, all other paths default to subdirectories under this path
# RUNTIME_BASE_PATH=E:/ComfyUIData
#
# Individual path overrides (these take precedence over RUNTIME_BASE_PATH):
# RUNTIME_UPLOADS_DIR=E:/Images          # Where uploaded images are stored
# RUNTIME_DATABASE_DIR=C:/Data/DB        # Where SQLite database files are stored
# RUNTIME_LOGS_DIR=C:/Logs               # Where log files are stored
# RUNTIME_TEMP_DIR=C:/Temp               # Where temporary files are stored
# RUNTIME_MODELS_DIR=E:/Models           # Where AI models are stored
# RUNTIME_RECYCLE_BIN_DIR=E:/Trash       # Where deleted files are moved
#
# Examples:
# - All data on D: drive: RUNTIME_BASE_PATH=D:/ComfyUI
# - Uploads only on E: drive: RUNTIME_UPLOADS_DIR=E:/Images
# - Network storage: RUNTIME_UPLOADS_DIR=//NAS/ComfyUI/uploads

# 업로드 설정
MAX_FILE_SIZE=52428800
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif,image/bmp,image/tiff

# ============================================
# Legacy settings (NOT USED - for reference only)
# ============================================
# These settings are no longer used by the application
# Path configuration is now handled by RUNTIME_* variables above
# DATABASE_PATH=../database/images.db
# UPLOAD_PATH=../uploads

# 보안 설정
JWT_SECRET=comfyui-image-manager-secret-2024
CORS_ORIGIN=http://localhost:1577

# 인증 설정
# Session secret for express-session (generate a random string for production)
SESSION_SECRET=your-session-secret-change-this-in-production

# 로깅 설정
LOG_LEVEL=info
LOG_FILE=logs/app.log

# WD v3 Tagger Settings
# Note: Tagger settings are managed via Settings UI (config/settings.json)
# The values below are only used as initial defaults on first run
# After first run, all tagger settings should be changed through Settings page